// What every generated component imports as `cn`: class names merged the Tailwind way, so a
// caller's `class` can override a component's default instead of fighting it. `cn` is
// shadcn-ui's own package and replaces the usual clsx + tailwind-merge pair.
export { cn } from 'cn'
