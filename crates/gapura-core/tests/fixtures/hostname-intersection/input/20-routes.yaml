apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: api, namespace: apps, generation: 1, creationTimestamp: "2026-09-01T10:00:00Z" }
spec:
  parentRefs: [{ name: main, namespace: infra }]
  hostnames: [api.example.com, other.org]
  rules:
  - backendRefs: [{ name: echo, port: 80 }]
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: nomatch, namespace: apps, generation: 1, creationTimestamp: "2026-09-01T10:00:00Z" }
spec:
  parentRefs: [{ name: main, namespace: infra }]
  hostnames: [other.org]
  rules:
  - backendRefs: [{ name: echo, port: 80 }]
