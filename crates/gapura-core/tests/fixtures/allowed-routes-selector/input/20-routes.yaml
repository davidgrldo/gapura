apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: ok, namespace: apps, generation: 1, creationTimestamp: "2026-09-01T10:00:00Z" }
spec:
  parentRefs: [{ name: main, namespace: infra }]
  rules:
  - matches: [{ path: { type: PathPrefix, value: / } }]
    backendRefs: [{ name: echo, port: 80 }]
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: denied, namespace: other, generation: 1, creationTimestamp: "2026-09-01T10:00:00Z" }
spec:
  parentRefs: [{ name: main, namespace: infra }]
  rules:
  - matches: [{ path: { type: PathPrefix, value: / } }]
    backendRefs: [{ name: echo, port: 80 }]
